[phases.setup]
nixPkgs = ["ta-lib", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install -g tsx",
  "cd openclaw/openclaw-acp && npm install --ignore-scripts || true"
]

[start]
cmd = "python -m src.bot"
