[phases.setup]
nixPkgs = ["ta-lib", "nodejs_20", "nodePackages.npm"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd openclaw/openclaw-acp && HUSKY=0 npm install --ignore-scripts"
]

[start]
cmd = "python -m src.bot"
